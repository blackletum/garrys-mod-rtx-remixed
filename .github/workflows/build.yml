name: Build

on:
  push:
    branches: [ main, gh-actions, sheepylord/PBR_gma_loader ]
  pull_request:
    branches: [ main, gh-actions ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    strategy:
      matrix:
        platform: [Win32, x64]
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2
    
    - name: Download Premake5
      run: |
        Invoke-WebRequest -Uri "https://github.com/premake/premake-core/releases/download/v5.0.0-beta2/premake-5.0.0-beta2-windows.zip" -OutFile "premake.zip"
        Expand-Archive -Path "premake.zip" -DestinationPath "."
      shell: pwsh
    
    - name: Generate project files
      run: .\premake5.exe --os=windows --gmcommon=./garrysmod_common vs2022
      shell: cmd
    
    - name: Build
      run: msbuild RTXFixesBinary.sln /p:Configuration=Release /p:Platform=${{ matrix.platform }} /m

    - name: Test Advanced Material Editor RTX writer
      if: matrix.platform == 'x64'
      run: |
        cmake -S source/advmat_rtx_bridge -B out/advmat-rtx-bridge -G "Visual Studio 17 2022" -A x64
        cmake --build out/advmat-rtx-bridge --config Release
        ctest --test-dir out/advmat-rtx-bridge -C Release --output-on-failure
      shell: pwsh
    
    - name: Prepare artifacts
      run: |
        if ("${{ matrix.platform }}" -eq "Win32") {
          $artifactName = "gmcl_rtxfixesbinary_win32.dll"
          $sourcePath = "x86\Release\gmcl_rtxfixesbinary_win32.dll"
        } else {
          $artifactName = "gmcl_rtxfixesbinary_win64.dll"
          $sourcePath = "x86_64\Release\gmcl_rtxfixesbinary_win64.dll"
        }
        New-Item -ItemType Directory -Force -Path "artifacts\garrysmod\lua\bin"
        Copy-Item $sourcePath "artifacts\garrysmod\lua\bin\$artifactName"
        Copy-Item "garrysmod\*" "artifacts\" -Recurse -Force
      shell: pwsh

    - name: Build and test Workshop PBR startup loader
      if: matrix.platform == 'x64'
      run: |
        cmake -S source/startup_assets -B out/startup -A x64
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
        cmake --build out/startup --config Release --parallel
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
        ctest --test-dir out/startup -C Release --output-on-failure
        if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
        # Stage beside the renderer: the launcher verifies the contract before
        # wrapping d3d9.dll. Never overwrite or distribute a real/fake renderer.
        $startupPackage = 'artifacts/bin/win64/astra-startup'
        New-Item -ItemType Directory -Force -Path $startupPackage
        Copy-Item out/startup/Release/d3d9.dll $startupPackage/
        Copy-Item out/startup/Release/astra_rtx_prepare_assets.exe $startupPackage/
        Copy-Item source/startup_assets/proxy_renderer_exports.json $startupPackage/
        Copy-Item source/startup_assets/generate_proxy_exports.py $startupPackage/
        Copy-Item docs/PBR_GMA_LOADER.md $startupPackage/README.md
      shell: pwsh
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: windows-${{ matrix.platform }}
        path: artifacts/*
        if-no-files-found: error

  create-nightly-release:
    needs: build-windows
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/gh-actions')
    permissions:
      contents: write
    
    steps:
    - name: Setup variables
      uses: actions/github-script@v7
      with:
        script: core.exportVariable('GITHUB_SHA_SHORT', context.sha.substring(0, 7))
    
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts
    
    - name: Package nightly release
      run: |
        mkdir -p nightly-release
        
        # Copy Win32 artifacts (includes bin/, garrysmod/, rtx-remix/, rtx.conf, dxvk.conf)
        if [ -d "artifacts/windows-Win32" ]; then
          cp -r artifacts/windows-Win32/* nightly-release/ || true
        fi
        
        # Copy x64 artifacts (merges with above)
        if [ -d "artifacts/windows-x64" ]; then
          cp -r artifacts/windows-x64/* nightly-release/ || true
        fi
        
        # Create zip with short hash in filename
        cd nightly-release
        zip -r ../garrys-mod-rtx-remixed-nightly-${GITHUB_SHA_SHORT}.zip .
        cd ..
    
    - name: Delete existing nightly release and tag
      uses: actions/github-script@v7
      continue-on-error: true
      with:
        script: |
          const { owner, repo } = context.repo;
          
          // Delete release first
          try {
            const release = await github.rest.repos.getReleaseByTag({
              owner,
              repo,
              tag: 'nightly'
            });
            await github.rest.repos.deleteRelease({
              owner,
              repo,
              release_id: release.data.id
            });
            console.log('Deleted existing nightly release');
          } catch (error) {
            console.log('No existing nightly release to delete:', error.message);
          }
          
          // Wait a moment for GitHub to process
          await new Promise(resolve => setTimeout(resolve, 1000));
          
          // Delete tag
          try {
            await github.rest.git.deleteRef({
              owner,
              repo,
              ref: 'tags/nightly'
            });
            console.log('Deleted existing nightly tag');
          } catch (error) {
            console.log('No existing nightly tag to delete:', error.message);
          }
          
          // Wait again to ensure deletion is complete
          await new Promise(resolve => setTimeout(resolve, 1000));
    
    - name: Create nightly release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: nightly
        name: "${{env.GITHUB_SHA_SHORT}} - Nightly"
        body: |
          Automated nightly build from commit ${{ github.sha }}
          
          **Commit:** ${{ github.event.head_commit.message }}
          **Branch:** ${{ github.ref_name }}
          **Build Date:** ${{ github.event.head_commit.timestamp }}
          
          ⚠️ This is an automated pre-release build and may be unstable.
        prerelease: true
        files: garrys-mod-rtx-remixed-nightly-${{env.GITHUB_SHA_SHORT}}.zip
